---
description: "Audit search experience from query intent and result promise through clarity, trust, interaction quality, accessibility signals, and conversion fit."
---

# seo-sxo-audit

This is a Claude Code command adapter.

Read and execute the canonical workflow spec at `.agents/workflows/seo-sxo-audit/workflow.md` and its contract at `.agents/workflows/seo-sxo-audit/contract.json`.

Pass the user's command arguments through as workflow input:

```text
$ARGUMENTS
```

Follow the workflow's required context, approval gates, verification gates, and return format. If this adapter conflicts with the canonical workflow spec, the canonical workflow spec wins.
