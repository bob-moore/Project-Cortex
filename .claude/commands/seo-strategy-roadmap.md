---
description: "Sequence approved SEO research, audits, clusters, and business priorities into a source-labeled strategy roadmap with owners, dependencies, and verification."
---

# seo-strategy-roadmap

This is a Claude Code command adapter.

Read and execute the canonical workflow spec at `.agents/workflows/seo-strategy-roadmap/workflow.md` and its contract at `.agents/workflows/seo-strategy-roadmap/contract.json`.

Pass the user's command arguments through as workflow input:

```text
$ARGUMENTS
```

Follow the workflow's required context, approval gates, verification gates, and return format. If this adapter conflicts with the canonical workflow spec, the canonical workflow spec wins.
