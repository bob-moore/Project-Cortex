---
description: "Turn keyword, SERP-intent, and content-audit evidence into topic clusters, hub/spoke architecture, page roles, boundaries, and internal-link recommendations."
---

# seo-topic-cluster

This is a Claude Code command adapter.

Read and execute the canonical workflow spec at `.agents/workflows/seo-topic-cluster/workflow.md` and its contract at `.agents/workflows/seo-topic-cluster/contract.json`.

Pass the user's command arguments through as workflow input:

```text
$ARGUMENTS
```

Follow the workflow's required context, approval gates, verification gates, and return format. If this adapter conflicts with the canonical workflow spec, the canonical workflow spec wins.
