---
description: "Create an evidence-backed SEO brief for a new page or existing-page refresh and hand it to the Writing discipline."
---

# seo-content-brief

This is a Claude Code command adapter.

Read and execute the canonical workflow spec at `.agents/workflows/seo-content-brief/workflow.md` and its contract at `.agents/workflows/seo-content-brief/contract.json`.

Pass the user's command arguments through as workflow input:

```text
$ARGUMENTS
```

Follow the workflow's required context, approval gates, verification gates, and return format. If this adapter conflicts with the canonical workflow spec, the canonical workflow spec wins.
