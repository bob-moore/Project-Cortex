---
description: "Compare a target domain with explicitly selected competitors using approved backlink evidence and route plausible authority opportunities."
---

# seo-competitor-backlink-gap

This is a Claude Code command adapter.

Read and execute the canonical workflow spec at `.agents/workflows/seo-competitor-backlink-gap/workflow.md` and its contract at `.agents/workflows/seo-competitor-backlink-gap/contract.json`.

Pass the user's command arguments through as workflow input:

```text
$ARGUMENTS
```

Follow the workflow's required context, approval gates, verification gates, and return format. If this adapter conflicts with the canonical workflow spec, the canonical workflow spec wins.
