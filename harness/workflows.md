---
date: 2026-08-01
description: Operator-facing matrix of canonical workflows, gates, and adapters.
tags:
  - harness
  - workflows
---

# Workflows

Canonical workflow behavior lives in `.agents/workflows/<workflow>/`. This file
is the readable registry for deciding whether a task belongs in a workflow,
skill, role, or vault-organization pass. See [[harness/skills]] for the
companion skill/discipline registry and [[harness/manual]] for the operating
rules these workflows follow.

## Registry Rules

- A workflow owns task shape, inputs, approval class, writes, verification, and
  return contract.
- Workflow resolution is mandatory when a request matches by name, adapter,
  alias, or clear task shape.
- If an apparent workflow match is skipped, the runtime must state why before
  continuing.
- A skill owns reusable procedure or reference material.
- A role owns capability boundaries.
- An adapter owns runtime discovery only.
- If a workflow row changes, regenerate adapters and run the gate.

## Adapter Parity

Current required adapters:

- Claude command: `.claude/commands/<workflow>.md`
- Codex workflow skill: `.agents/skills/workflow-<workflow>/SKILL.md`

Gemini and Hermes workflow adapters remain pending until their invocation
schemas are defined and verifiable.

## Writing Workflows

| Workflow | Purpose | Primary Skills | Approval Classes | Verification | Done Signal |
| --- | --- | --- | --- | --- | --- |
| `write-web-copy` | Draft standard or promotional website copy. | `writing-foundation`, `web-copy`, `claim-check`, `writing-quality-gate` | `read_only`, `vault_write`, `external_read` | Writing discipline followed; Brand/Voice gaps surfaced; material claims checked. | Web intent explicit; artifact returned or saved; claims labeled; gate passed or deferred with reason. |
| `write-email-newsletter` | Draft email/newsletter copy without sending. | `writing-foundation`, `email-newsletter`, `claim-check`, `writing-quality-gate` | `read_only`, `vault_write`, `external_read` | Writing discipline followed; no send/schedule/import/automation mutation implied. | Email intent explicit; subject, preview, body, CTA returned or saved; no-send boundary visible. |
| `write-article` | Create a long-form article through brief, outline, draft, and quality gate. | `article-brief`, `article-outline`, `article-draft`, `claim-check`, `writing-quality-gate` | `read_only`, `vault_write`, `external_read` | Claims checked; SEO/AEO/schema only when goal-driven; edited files verified. | Article type and reader intent explicit; artifact returned or saved; claims labeled; gate passed or deferred. |
| `refresh-article` | Audit and update an existing long-form article. | `article-refresh`, `claim-check`, `copy-edit`, `writing-quality-gate` | `read_only`, `vault_write`, `external_read` | Read-first audit precedes rewrite; claim and freshness changes justified. | Existing article inspected; preserved and changed material separated; refreshed artifact or audit returned or saved. |
| `repurpose-content` | Turn an existing artifact into derivative channel outputs. | `content-repurpose`, target writing skill, `claim-check`, `writing-quality-gate` | `read_only`, `vault_write`, `external_read` | Source map exists; reused/compressed claims checked; unsupported claims are not strengthened. | Source inspected; target channels explicit; outputs returned or saved; evidence caveats preserved. |

## Design Workflows

| Workflow | Purpose | Primary Skills | Approval Classes | Verification | Done Signal |
|---|---|---|---|---|---|
| `design-research` | Research and narrow a visual direction through a routed moodboard, annotated references, anti-SaaS checks, and explicit alternatives before implementation. | `design-research`, `brandkit`, `redesign-existing-projects` | `read_only`, `vault_write`, `external_read` | Sources, provenance, observations, anti-SaaS assessment, alternatives, approval state, and packet links verified. | Research packet is returned or saved; direction remains awaiting approval; next handoff is explicit. |

## SEO Workflows

| Workflow | Purpose | Primary Skills | Approval Classes | Verification | Done Signal |
| --- | --- | --- | --- | --- | --- |
| `seo-content-brief` | Create a new-page or refresh SEO brief from approved client context and search evidence, then hand requirements to Writing. | `seo-content-brief`, `seo-quality-gate`, `article-brief` / `web-copy` / `article-refresh` | `read_only`, `vault_write`, `external_read` | Onboarding context, mode, source/date scope, intent, opportunity rationale, Writing handoff, and quality gate verified. | Approved brief saved in resolved context or intake is required; no final prose or metadata produced. |
| `seo-keyword-research` | Research, expand, cluster, and prioritize SEO keywords and topics with runtime provider availability checks and source-labeled intent evidence. | `seo-keyword-research`, `keyword-research-and-clustering`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Provider statuses, scope, sources/dates, intent, clusters, priorities, and handoffs verified. | Keyword research artifact saved or intake/approval is required; no final copy or metadata produced. |
| `seo-serp-intent` | Analyze bounded search-result pages to classify intent, SERP composition, result overlap, and page-type fit with source-labeled evidence. | `seo-serp-intent`, `seo-keyword-research`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Query/source scope, provider status, SERP observations, ranking-page inspection, intent, page fit, and quality gate verified. | SERP intent artifact saved or approval is required; no final copy or metadata produced. |
| `seo-content-audit` | Audit existing content for SEO query and SERP fit, freshness, decay signals, overlap, and refresh or consolidation recommendations. | `seo-content-audit`, `seo-content-brief`, `seo-keyword-research`, `seo-serp-intent`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Content scope, provider status, page observations, query fit, decay evidence, actions, handoffs, and quality gate verified. | Content audit artifact saved or approval is required; no final copy, deletion, redirect, or publishing action performed. |
| `seo-topic-cluster` | Turn keyword, SERP-intent, and content-audit evidence into topic clusters, hub/spoke architecture, page roles, boundaries, and internal-link recommendations. | `seo-topic-cluster`, `seo-keyword-research`, `seo-serp-intent`, `seo-content-audit`, `seo-content-brief`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Input artifacts, cluster rationale, page roles, boundaries, internal-link recommendations, handoffs, and quality gate verified. | Cluster artifact saved or required input is requested; no final copy or link implementation performed. |
| `seo-strategy-roadmap` | Sequence approved SEO research, audits, clusters, and business priorities into a source-labeled strategy roadmap with owners, dependencies, and verification. | `seo-strategy-roadmap`, `seo-keyword-research`, `seo-serp-intent`, `seo-content-audit`, `seo-topic-cluster`, `seo-content-brief`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Evidence register, objectives, initiatives, sequence, owners, dependencies, handoffs, and quality gate verified. | Roadmap artifact saved or required context is requested; no final copy, code, CMS, or production mutation performed. |
| `seo-backlink-audit` | Audit bounded external-link profiles with source-labeled provider, crawl, first-party, and manual evidence. | `seo-backlink-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Provider status, freshness, profile metrics, link context, quality signals, action queue, and quality gate verified. | Backlink audit artifact saved or approval is required; no link buying, outreach, disavowal, or production mutation performed. |
| `seo-link-building-opportunities` | Turn approved backlink, content, topic, brand, and market evidence into ethical, prioritized link-building opportunities without outreach or link acquisition. | `seo-link-building-opportunities`, `seo-backlink-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Source status, opportunity rationale, destination, asset/proof, effort, risk, owner, handoffs, and quality gate verified. | Opportunity artifact saved or approval is required; no outreach, link acquisition, or production mutation performed. |
| `seo-competitor-backlink-gap` | Compare a target domain with explicitly selected competitors using approved backlink evidence and route plausible authority opportunities. | `seo-competitor-backlink-gap`, `seo-backlink-audit`, `seo-link-building-opportunities`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Explicit domains, provider status, freshness, shared/distinct patterns, opportunities, approval, and quality gate verified. | Comparison artifact saved or competitor/provider approval is required; no competitor contact, outreach, link acquisition, or production mutation performed. |
| `seo-link-reclaim` | Identify broken, lost, redirected, orphaned, or unlinked external-link candidates and route safe reclaim recommendations. | `seo-link-reclaim`, `seo-backlink-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Source status, historical/current status, evidence, least-destructive action, owner, recheck, handoffs, and quality gate verified. | Reclaim artifact saved or approval is required; no redirect, CMS, outreach, disavowal, or production mutation performed. |
| `seo-local-audit` | Audit local SEO for business identity, NAP, service areas, local intent, location pages, reviews, citations, and local evidence. | `seo-local-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Business scope, source status, NAP/business facts, local intent, listings/reviews/citations, actions, and quality gate verified. | Local audit artifact saved or approval is required; no listing, review, citation, CMS, or production mutation performed. |
| `seo-maps-audit` | Audit approved maps and local-listing evidence for identity, categories, service areas, local-pack signals, and landing-page fit. | `seo-maps-audit`, `seo-local-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Location/query scope, source status, profile/map signals, provider limits, page fit, actions, and quality gate verified. | Maps artifact saved or approval is required; no profile, listing, review, citation, CMS, or production mutation performed. |
| `seo-citation-audit` | Audit approved local citation evidence for NAP, category, URL, ownership, relevance, indexability, duplicates, freshness, and correction priority. | `seo-citation-audit`, `seo-local-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Business facts, source status, freshness, evidence, confidence, correction queue, owners, and quality gate verified. | Citation artifact saved or approval is required; no listing submission, edit, claim, suppression, deletion, or production mutation performed. |
| `seo-review-signals` | Assess approved local review evidence for volume, recency, relevance, sentiment themes, response coverage, and safe operational handoffs. | `seo-review-signals`, `seo-local-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Review scope, source status, sample/method, confidence, themes, handoffs, and quality gate verified. | Review artifact saved or approval is required; no review, profile, platform, CMS, or production mutation performed. |
| `seo-location-page-audit` | Audit location and service-area pages for local usefulness, business purpose, intent fit, facts, internal links, schema, and doorway risk. | `seo-location-page-audit`, `seo-local-audit`, `seo-content-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Page scope, facts, local value, intent fit, duplication/thinness, doorway risk, handoffs, and quality gate verified. | Location-page artifact saved or approval is required; no URL, CMS, schema, code, copy, or production mutation performed. |
| `seo-geo-audit` | Audit generative-search and AI Overview readiness with source-tier labels and no unsupported visibility claims. | `seo-geo-audit`, `seo-content-audit`, `seo-serp-intent`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Evidence register, source tiers, answerability, entity clarity, citation readiness, handoffs, and quality gate verified. | GEO artifact saved or approval is required; no AI visibility claim, content, schema, code, or production mutation performed. |
| `seo-aeo-audit` | Audit answer-engine and SERP answer-feature readiness with source labels and no unsupported placement or visibility claims. | `seo-aeo-audit`, `seo-serp-intent`, `seo-content-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Query/page scope, answer-feature evidence, source tiers, answerability, handoffs, and quality gate verified. | AEO artifact saved or approval is required; no answer placement claim, content, schema, code, or production mutation performed. |
| `seo-sxo-audit` | Audit search experience from query intent and result promise through clarity, trust, interaction quality, accessibility signals, and conversion fit. | `seo-sxo-audit`, `seo-serp-intent`, `seo-content-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Journey scope, evidence tiers, intent fit, clarity, trust, interaction, accessibility signals, handoffs, and quality gate verified. | SXO artifact saved or approval is required; no copy, design, CMS, code, analytics, or production mutation performed. |
| `seo-ai-visibility-check` | Run bounded, explicitly scoped AI/search visibility observations with source, approval, and reproducibility labels. | `seo-ai-visibility-check`, `seo-geo-audit`, `seo-aeo-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Engine/model scope, approval, observation log, citations/mentions, reproducibility, limitations, handoffs, and quality gate verified. | Visibility artifact saved or approval is required; no universal visibility claim, provider mutation, content, code, or production mutation performed. |
| `seo-ecommerce-audit` | Audit ecommerce product, collection, marketplace, inventory, pricing, review, template, and indexability evidence. | `seo-ecommerce-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Commerce scope, source tiers, product/collection findings, template risks, owners, and quality gate verified. | Ecommerce artifact saved or approval is required; no catalog, feed, schema, CMS, code, or production mutation performed. |
| `seo-product-schema` | Audit product, offer, review, aggregate rating, availability, identifier, variant, and merchant schema facts. | `seo-product-schema`, `seo-ecommerce-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Product scope, schema facts, first-party evidence, validation, owners, and quality gate verified. | Product-schema artifact saved or approval is required; no schema, feed, CMS, code, or production mutation performed. |
| `seo-collection-page-audit` | Audit ecommerce collection/category pages for intent, assortment relevance, information gain, unique value, pagination, filters, and template risk. | `seo-collection-page-audit`, `seo-ecommerce-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Catalog/template scope, intent, information gain, duplication/thinness, owners, and quality gate verified. | Collection artifact saved or approval is required; no page, filter, canonical, CMS, code, or production mutation performed. |
| `seo-faceted-navigation-audit` | Audit ecommerce facets, parameters, crawl paths, canonicals, indexation, links, and crawl traps at explicit sample scope. | `seo-faceted-navigation-audit`, `seo-ecommerce-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Facet scope, crawl caps, sample, index/canonical evidence, risks, owners, and quality gate verified. | Facet artifact saved or approval is required; no robots, canonical, parameter, sitemap, CMS, code, or production mutation performed. |
| `seo-programmatic-quality-gate` | Block unsafe scaled SEO recommendations until samples, information gain, business purpose, uniqueness, evidence, and controls are verified. | `seo-programmatic-quality-gate`, `seo-ecommerce-audit`, `seo-collection-page-audit`, `seo-faceted-navigation-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Samples, purpose, information gain, uniqueness, risk, controls, verdict, approvals, and recheck verified. | Gate artifact saved or approval is required; no page generation, publishing, template, CMS, code, or production mutation performed. |
| `seo-google-data` | Define bounded Google SEO data collection for GSC, GA4, PSI, CrUX, URL Inspection, and Indexing API with status and freshness labels. | `seo-google-data`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Credential, tool, access, freshness, scope, partial state, evidence, and quality gate verified. | Data artifact saved or approval is required; no analytics, property, indexing, or production mutation performed. |
| `seo-dataforseo` | Define bounded DataForSEO evidence handling with provider availability, credential, quota, freshness, market, language, device, and methodology labels. | `seo-dataforseo`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Provider status, quota, endpoint, freshness, scope, methodology, evidence, and quality gate verified. | Provider artifact saved or approval is required; no paid call or provider mutation without approval. |
| `seo-drift-baseline` | Create approved pre-change SEO drift baselines with explicit scope, evidence paths, freshness, sampling, and reproducibility. | `seo-drift-baseline`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Baseline ID, change context, source status, snapshot scope, freshness, caps, and integrity verified. | Baseline artifact saved or approval is required; no live-system mutation or baseline overwrite performed. |
| `seo-drift-compare` | Compare approved SEO drift baselines and after-state snapshots, separating observed changes from hypotheses and confounders. | `seo-drift-compare`, `seo-drift-baseline`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Matching scope, before/after evidence, deltas, thresholds, missingness, confounders, and rechecks verified. | Compare artifact saved or approval is required; no causal or production claim without evidence. |
| `seo-weekly-monitor` | Define recurring read-only SEO monitoring with sources, freshness, thresholds, stale-data handling, escalation, and retention. | `seo-weekly-monitor`, `seo-google-data`, `seo-dataforseo`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Cadence, source status, freshness SLA, thresholds, partial handling, alerts, retention, and escalation verified. | Monitor artifact saved or approval is required; no automatic analytics, provider, CMS, code, SEO, or production mutation. |
| `seo-report` | Produce a complete Markdown SEO report from approved evidence with optional presentation output. | `seo-report`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Scope, evidence, findings, actions, appendix, limitations, source labels, and quality gate verified. | Markdown report saved or approval is required; no live reads or external mutation by implication. |
| `seo-action-plan` | Convert verified SEO findings into prioritized, assignable actions with dependencies, approvals, risks, and rechecks. | `seo-action-plan`, `seo-report`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Finding links, priority, owner, rationale, dependency, approval, risk, verification, status, and recheck verified. | Action plan saved or approval is required; no action is treated as approved implementation. |
| `seo-executive-summary` | Produce a client-readable SEO decision summary that separates facts, interpretation, recommendations, assumptions, and unknowns. | `seo-executive-summary`, `seo-report`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Period, material findings, risks, opportunities, decisions, limitations, source labels, and quality gate verified. | Summary saved or approval is required; no ranking, traffic, lead, revenue, or recovery guarantee. |
| `seo-client-roadmap` | Sequence approved SEO work into a dependency-aware client roadmap with outcomes, owners, approvals, evidence prerequisites, and rechecks. | `seo-client-roadmap`, `seo-action-plan`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Phases, outcomes, dependencies, owners, approvals, prerequisites, definition of done, statuses, and rechecks verified. | Roadmap saved or approval is required; no invented dates, budgets, staffing, or commitments. |
| `seo-monitoring-pack` | Package recurring SEO signals, drift, alerts, source freshness, actions, limitations, escalation, and next runs into a scorecard. | `seo-monitoring-pack`, `seo-weekly-monitor`, `seo-drift-compare`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Source status, signals, thresholds, drift, alerts, freshness, partial handling, actions, retention, and escalation verified. | Monitoring pack saved or approval is required; no automatic analytics, provider, CMS, code, SEO, or production mutation. |
| `seo-page-audit` | Run a bounded single-page SEO evidence audit with neutral CLI tools. | `seo-foundation`, `seo-tool-runner`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Raw and normalized artifacts preserved; evidence labels and limitations visible; quality gate evaluated. | Target and scope explicit; audit artifacts saved or blocker reported; return contract complete. |
| `seo-technical-audit` | Run a bounded site-level technical SEO evidence audit with explicit crawl caps. | `seo-foundation`, `seo-tool-runner`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Crawl caps, sitemap/robots limits, raw artifacts, affected URL samples, and partial states visible; quality gate evaluated. | Site and caps explicit; bounded audit artifacts saved or blocker reported; return contract complete. |
| `seo-site-audit` | Orchestrate a bounded site audit from technical, page, and Phase 3 specialist checks. | `seo-foundation`, `seo-technical-audit`, `seo-page-audit`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Unified evidence root; caps, partial states, specialist checks, and quality gate visible. | Site contract explicit; bounded evidence bundle saved or blocker reported; return contract complete. |
| `seo-sitemap` | Inspect a sitemap or sitemap index without mutation. | `seo-foundation`, `seo-tool-runner`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | XML parse status, missing/duplicate locs, source and limits preserved. | Sitemap evidence returned or blocker reported; no generation/submission performed. |
| `seo-schema` | Detect and structurally inspect JSON-LD without deployment. | `seo-foundation`, `seo-tool-runner`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | JSON syntax, observed types, source facts, and implementation handoff verified. | Schema evidence returned or blocker reported; no rich-result promise. |
| `seo-images` | Inspect image markup for alt and layout-related attributes. | `seo-foundation`, `seo-tool-runner`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Image inventory, omitted/empty alt, and dimensions distinguished. | Image evidence returned or blocker reported; no asset mutation. |
| `seo-hreflang` | Inspect hreflang alternate links without changing international routing. | `seo-foundation`, `seo-tool-runner`, `seo-quality-gate` | `read_only`, `vault_write`, `external_read` | Codes, duplicates, href values, reciprocity limits, and locale assumptions visible. | Hreflang evidence returned or blocker reported; no routing mutation. |

## Operations Workflows

| Workflow | Purpose | Primary Roles | Approval Classes | Verification | Done Signal |
| --- | --- | --- | --- | --- | --- |
| `assistant` | Answer a freeform operational evidence request about the User's calendar, email, or Monday.com workload without making planning decisions. | `assistant` | `read_only`, `external_read`, `external_mutation` | Source labels and retrieval time present; calendar mutations read back after approval; no vault writes. | Evidence-backed answer returned or blocker reported; no planning decision made. |

## Vault Workflows

| Workflow | Purpose | Primary Roles | Approval Classes | Verification | Done Signal |
| --- | --- | --- | --- | --- | --- |
| `vault-audit` | Audit vault structure, indexes, frontmatter, links, Bases, and consistency. | `vault-librarian`, `cross-linker` | `read_only`, `vault_write` | Changed Markdown/frontmatter and wikilinks verified. | Scope satisfied or blocked; touched files listed; approval gates respected. |
| `vault-capture-1on1` | Convert 1:1 material into structured notes and related context. | `operator` | `vault_write`, `external_read` | External evidence labeled; changed files and links verified. | Evidence labeled; touched files listed; return contract complete. |
| `vault-dump` | Route freeform captured material into the right vault notes. | `operator` | `vault_write` | Changed files and links verified. | Scope satisfied or blocked; touched files listed. |
| `vault-humanize` | Voice-calibrated editing for vault text. | `operator` | `vault_write` | Changed files and links verified; character count checked when needed. | Edited file returned or saved; limits respected. |
| `vault-incident-capture` | Capture incidents from Slack or other evidence into structured notes. | `slack-archaeologist`, `people-profiler` | `vault_write`, `external_read` | External evidence labeled; changed files and links verified. | Timeline/evidence captured; touched files listed. |
| `vault-intake` | Process unread `inbox/` items into the vault and clear handled items. | `operator` | `vault_write` | Changed files and links verified. | Inbox items classified, routed, and reported. |
| `vault-kickoff` | Brief and dispatch selected client-work roles while retaining verification ownership. | `assistant`, `strategist`, `writer`, `designer`, `developer`, `wordpress-operator` | `read_only`, `vault_write`, `external_read`, `external_mutation`, `destructive`, `production` | External evidence labeled; parent verifies role evidence before closure. | Selected work completed or blocked under approval gates. |
| `vault-meeting` | Prepare for a meeting by topic with context, blockers, and agenda. | `operator` | `read_only` | Return contract checked. | Brief returned with scope satisfied or blocked. |
| `vault-onboard` | First-run conversational setup: interview the user, define the Operator, check dependencies. | `operator` | `read_only`, `vault_write` | Unconfigured markers actually removed, not just reported done; changed files and links verified. | Applicable phases completed or explicitly skipped/blocked; touched files listed. |
| `vault-peer-scan` | Scan a peer's GitHub PRs for review evidence. | `operator` | `external_read`, `vault_write` | External evidence labeled; changed files and links verified. | Evidence saved under `reviews/evidence/`; touched files listed. |
| `vault-prep-1on1` | Prepare for an upcoming 1:1. | `operator` | `read_only`, `external_read` | External evidence labeled when used. | Agenda/context returned; no write required. |
| `vault-project-archive` | Archive completed projects or ended client relationships. | `operator` | `vault_write`, `destructive` | Archive, index, and status alignment confirmed. | Project moved/statused without broken retrieval paths. |
| `vault-review-brief` | Generate review context-transfer briefs. | `review-prep` | `vault_write` | Changed files and links verified; character counts checked when needed. | Brief saved or returned with touched files listed. |
| `vault-review-peer` | Draft peer reviews from vault evidence. | `operator` | `vault_write` | Changed files and links verified; character counts checked when needed. | Draft saved or returned with limits respected. |
| `vault-self-review` | Draft self-review material from vault evidence. | `operator` | `vault_write` | Changed files and links verified; character counts checked when needed. | Draft saved or returned with limits respected. |
| `vault-slack-scan` | Scan Slack for person or project context. | `operator` | `external_read`, `vault_write` | External evidence labeled; changed files and links verified. | Vault-ready evidence/context returned or saved. |
| `vault-standup` | Morning kickoff from task, calendar, and vault context. | `assistant` | `external_read`, `vault_write` | External evidence labeled; `Home.md` changes verified. | Priorities and touched files reported. |
| `vault-stash-session` | Create a compact resume note for continuing paused or interrupted vault work in a later runtime session. | `operator`, `vault-librarian` | `read_only`, `vault_write` | Resume note exists; required sections present; `harness/memory.md` links to resume notes. | Dated resume note saved under `harness/resume/`; future resume prompt returned. |
| `vault-weekly` | Weekly synthesis across activity, North Star, patterns, candidate review evidence, and priorities. | `operator` | `read_only`, `vault_write`, `external_read` | External evidence labeled; changed files and links verified. | Weekly synthesis saved or returned. |
| `vault-wrap-up` | End-of-session vault review and memory/workflow cleanup. | `review-evidence` when review evidence is in scope | `read_only`, `vault_write` | Changed files and links verified. | Session changes reviewed; touched files listed. |

## Validation Command

Run this before declaring workflow architecture stable:

```sh
node .agents/scripts/gate.mjs
```
